-- pgvector migration placeholder
-- The vector extension and column conversion must be applied manually
-- on databases where pgvector is available.
-- On local dev without pgvector, memory search falls back to raw message history.
SELECT 1;
